name: day22_firestore_crud
description: "Firestore CRUD (Create, Read, Update, Delete) using GetX + MVC"
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ^3.13.1
dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.2
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
